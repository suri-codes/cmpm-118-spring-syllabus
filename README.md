# Syllabus

Use `nix develop` to enter the development environment.

If you don't have the `nix` package manager installed, you will
need the `just` command runner and the `typst` compiler.

Running `just` will continuously compile and open
the PDF.

